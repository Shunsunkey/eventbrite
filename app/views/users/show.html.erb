<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h2>Mon profil</h2>

      <div class="field">
        <strong>Email:</strong>
        <%= @user.email %>
      </div>

      <div class="field">
        <strong>Email EventBrite:</strong>
        <%= @user.fake_email %>
      </div>

      <div class="field">
        <strong>Prénom:</strong>
        <%= @user.first_name %>
      </div>

      <div class="field">
        <strong>Nom de famille:</strong>
        <%= @user.last_name %>
      </div>

      <div class="field">
        <strong>Âge:</strong>
        <%= @user.age %>
      </div>

      <div class="field">
        <strong>Description:</strong>
        <%= @user.description %>
      </div>

      <%= link_to "Modifier mon profil", edit_user_registration_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>
